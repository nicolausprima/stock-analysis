services:
  dashboard:
    build: .
    container_name: stock-ai-dashboard
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn dashboard.backend.main:app --host 0.0.0.0 --port 8000
